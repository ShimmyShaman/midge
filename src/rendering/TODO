TODO

Today:
  >> Increase my productivity
    >> Create midge to be more productive tool for me to use than vs-code
      >> Use the editor to create / prototype the init process
        >> data recording?
        >> create process:
          >> recognises code-block/statement-list context
          >> sees struct-name suggested
          >> init struct-name process













> do relative directory stuff with a ignored config home directory
> do better thread handling of errors and reporting for off-threads